#include "BuyAndHold.h"

double BuyAndHold::onBar(const PriceSeries& series, std::size_t i) { // BAH::onBar indiates this is the onBar for BAH ONLY
    // body: express buy-and-hold as a target position
}